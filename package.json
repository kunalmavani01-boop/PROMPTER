{
  "name": "prompter",
  "version": "1.0.0",
  "description": "Prompt optimization toolkit with FastAPI backend and browser UI",
  "private": true,
  "scripts": {
    "start": "python -m uvicorn main:app --host 127.0.0.1 --port 8000",
    "dev": "python -m uvicorn main:app --reload --host 127.0.0.1 --port 8000"
  }
}
